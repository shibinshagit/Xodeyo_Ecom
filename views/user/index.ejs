<%- include('../layouts/head.ejs') %>

<body>
<%- include('../layouts/loginBanner.ejs') %>
<%- include('../layouts/header.ejs') %>


    <main class="main">   
        <section class="home-slider position-relative">            
            <div class="hero-slider-1 dot-style-1 dot-style-1-position-1">
                <% for (let i=0; i < bannersPrimary.length; i++) { %>
                <div class="single-hero-slider single-animation-wrap">
                    <div class="container">
                        <div class="row align-items-center slider-animated-1">
                            <div class="col-lg-5 col-md-6">
                                <div class="hero-slider-content-2">
                                    <h4 class="animated"><%= bannersPrimary[i].head %></h4>
                                    <h2 class="animated fw-900"><%= bannersPrimary[i].subHead %></h2>
                                    <h1 class="animated fw-900 text-brand"><%= bannersPrimary[i].mainHead %></h1>
                                    <p class="animated"><%= bannersPrimary[i].note %></p>
                                    <a class="animated btn btn-brush btn-brush-3" href="/shop"> Shop Now </a>
                                </div>
                            </div>
                            <div class="col-lg-7 col-md-6">
                                <div class="single-slider-img single-slider-img-1">
                                    <img class="animated slider-1-1" src="assets/imgs/bannerImages/<%= bannersPrimary[i].Image %>" alt="">
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <%}%>
            </div>
            <div class="slider-arrow hero-slider-1-arrow"></div>
            
        </section>
        <section class="popular-categories section-padding mt-15">
        <div class="container wow fadeIn animated">
            <h3 class="section-title mb-20"><span>All</span> Categories</h3>
            <div class="carausel-6-columns-cover position-relative">
                <div class="slider-arrow slider-arrow-2 carausel-6-columns-arrow" id="carausel-6-columns-arrows"></div>
                <div class="carausel-6-columns" id="carausel-6-columns">
                    <% for (let i=0; i < categories.length; i++) { %>
                    <div class="card-1">
                        <figure class=" img-hover-scale overflow-hidden">
                            <a href="/shop?category=<%= categories[i].category %>"><img src="assets/imgs/categoryImages/<%= categories[i].image %>" alt=""></a>
                        </figure>
                    </div>
                    <%}%>
                </div>
            </div>
        </div>
    </section>
    <% for (let i=0; i < bannersSecondary.length; i++) { %>
    <section class="mb-50">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <div class="banner-bg wow fadeIn animated" style="background-image: url('assets/imgs/bannerImages/<%= bannersSecondary[i].Image %>')">
                        <div class="banner-content">
                            <h5 class="text-grey-4 mb-15"><%= bannersSecondary[i].head %></h5>
                            <h2 class="fw-600"><%= bannersSecondary[i].subHead %> <span class="text-brand"><%= bannersSecondary[i].mainHead %></span><%= bannersSecondary[i].note %></h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
  
    <%}%>

  
      

        <section class="product-tabs section-padding position-relative wow fadeIn animated">
            <div class="bg-square"></div>
            <div class="container">
                <div class="tab-header">
                    <h3 class="section-title mb-20"><span>New</span>Arrivals</h3>
                </div>
                <!--End nav-tabs-->
               
                <div class="tab-content wow fadeIn animated" id="myTabContent">
                    <div class="tab-pane fade show active" id="tab-one" role="tabpanel" aria-labelledby="tab-one">
                        <div class="row product-grid-4">
                            <% for (let i=0; i < products.length; i++) { %>
                            <div onclick="window.location.href='/showproducts?id=<%= products[i]._id.toString() %>'" class="col-lg-3 col-md-4 col-12 col-sm-6">
                                <div class="product-cart-wrap mb-30">
                                    <div class="product-img-action-wrap">
                                        <div class="product-img product-img-zoom">
                                            <a href="/showproducts?id=<%= products[i]._id.toString() %>">
                                                <img class="default-img" src="assets/imgs/productImages/<%= products[i].images[0] %>" alt="">
                                                <img class="hover-img" src="assets/imgs/productImages/<%= products[i].images[1] %>" alt="">
                                            </a>
                                        </div>
                                        <div class="product-action-1">
                                            <a aria-label="Add To Wishlist" class="action-btn hover-up" href="/addtowishlist?id=<%= products[i]._id.toString() %>"  onclick="addToWishlist(event)"><i class="fi-rs-heart"></i></a>
                                            <audio id="successSound" src="assets/audios/Voicy_PhHub x See you Again.mp3"></audio>
                                        </div>
                                       
                                        <% if (new Date(products[i].createdAt) > new Date() - 7 * 24 * 60 * 60 * 1000) { %>
                                            <div class="product-badges product-badges-position product-badges-mrg">
                                                <span class="new">new</span>
                                            </div>
                                        <% } else if (1 === 1) { %>
                                            <%
                                            const p = products[i].price / 100;
                                            const d = products[i].price - products[i].discountRate;
                                            const result = (d / p).toFixed(0);
                                        %>
                                        
                                        <div class="product-badges product-badges-position product-badges-mrg">
                                            <span class="hot"><%= result %>%</span>
                                        </div>
                                        
                                            
                                        <% } else if (1 === 2) { %>
                                            <div class="product-badges product-badges-position product-badges-mrg">
                                                <span class="sale">hot</span>
                                            </div>
                                        <% } else if (2 === 3) { %>
                                            <div class="product-badges product-badges-position product-badges-mrg">
                                                <span class="best">Top</span>
                                            </div>
                                        <% } %>
                                        
                                    </div>
                                    <div class="product-content-wrap">
                                        <div class="product-category">
                                            <a href=""><%= products[i].productCategory %></a>
                                        </div>
                                        <h2><a href=""><%= products[i].productName %></a></h2>
                                        <!-- <div class="rating-result" title="90%">
                                            <span>
                                                <span>90%</span>
                                            </span>
                                        </div> -->
                                        <div class="product-price">
                                            <% if ( products[i].discountRate !==products[i].price && new
                                                    Date() <=new Date(products[i].discountEnd) && new Date()>= new
                                                    Date(products[i].discountStart) &&
                                                    products[i].discountStatus==true)
                                                    { %>
                                                        <span>₹<%= products[i].discountRate %></span>

                                                    <span class="old-price"><%= products[i].price %></span>
                                                    <% } else { %>
                                                        <span> <%= products[i].price %></span>
                                                            <% } %>
                                            
                                           
                                        </div>
                                        <!-- <div class="product-action-1 show">
                                            <a aria-label="Add To Cart" class="action-btn hover-up" href="shop-cart.html"><i class="fi-rs-shopping-bag-add"></i></a>
                                        </div> -->
                                    </div>
                                </div>
                            </div>
                           
                           <%}%>
                        </div>
                    </div>
                </div>
                </div>
        </section>

       

    </main>    

    <%- include('../layouts/footer.ejs') %>

    <script>
        async function addToWishlist(event) {
          // Prevent the default link behavior
          event.preventDefault();
          event.stopPropagation();
      
          // Get the href attribute from the clicked link
          const href = event.currentTarget.getAttribute('href');
      
          try {
            // Use fetch to send a request to the server
            const response = await fetch(href, {
              method: 'GET', // or 'POST' depending on your server endpoint
            });
      
            // Check if the response was successful (status code 200-299)
            if (response.ok) {
              const data = await response.json(); // Assuming the response is JSON
              handleResponse(data);
            } else {
              handleError();
            }
          } catch (error) {
            console.error('Error:', error);
            handleError();
          }
        }
      
        function handleResponse(data) {
          // Display SweetAlert based on the response
          if (data.success) {
//             Swal.fire({
//   icon: 'success',
//   title: '💖 Love Alert! 💖',
//   text: 'You just added a product to your wishlist!',
//   imageUrl: 'https://example.com/love-image.gif', // Add a URL to a love-themed image
//   imageWidth: 200, // Adjust the width of the image
//   imageHeight: 200, // Adjust the height of the image
//   showConfirmButton: false,
//   timer: 3000, // Adjust the timer as needed
//   background: '#ffe6f2', // Adjust the background color to a lovely tone
//   customClass: {
//     popup: 'popping-love-popup', // Add a custom class for additional styling
//     title: 'popping-love-title', // Style the title if needed
//     content: 'popping-love-content' // Style the content if needed
//   }
// });

            const successSound = document.getElementById('successSound');
            successSound.play();
          } else {
            Swal.fire({
              icon: 'info',
              title: 'already in your wishlist',
              text: 'buy now!',
              confirmButtonText: 'OK',
            });
          }
      
          // You can perform additional actions based on the response data if needed
        }
      
        function handleError() {
          // Display error SweetAlert
          Swal.fire({
            icon: 'error',
            title: 'Failed to add to wishlist',
            text: 'Please try again later.',
            confirmButtonText: 'OK',
          });
        }
      </script>
      






























  